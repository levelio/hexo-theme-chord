if page.next || page.prev
  .post_paginator
    if page.prev
      a.pre(href=url_for(page.prev_link || page.prev.path))= page.prev.title
    if page.next
      a.next(href=url_for(page.next_link || page.next.path))= page.next.title
