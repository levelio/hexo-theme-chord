if page.prev || page.next
  .paginator
    if page.prev
      a.pre(href=url_for(page.prev_link || page.prev.path))= __('previous')

    if page.next
      a.next(href=url_for(page.next_link || page.next.path))= __('next')